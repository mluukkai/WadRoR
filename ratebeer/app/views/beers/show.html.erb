<p id="notice"><%= notice %></p>

<h2>
  <%= @beer %>
</h2>

<span class="label label-info"> <%= @beer.style %></span>

<p></p>

<% if @beer.ratings.empty? %>
  <p>beer does not have yet been rated!</p>
<% else %>
  <p>has been rated <%= @beer.ratings.count %> times, average score <%= @beer.average_rating %></p>
<% end %>

<h4>give a rating:</h4>

<%= form_for(@rating) do |f| %>
  <%= f.hidden_field :beer_id %>
  score: <%= f.number_field :score %>
  <%= f.submit :class => "btn btn-primary"%>
<% end %>

<p></p>

<%= link_to 'Edit', edit_beer_path(@beer), :class => "btn btn-primary" %>

<%= destroy_button(@beer) %>
